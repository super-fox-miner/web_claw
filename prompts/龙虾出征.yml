interaction: |
  请根据用户需求，严格按照以下工作流程执行：

  ## 工作流程（必须遵守）
  1. 将用户需求转换为具体的任务流程，并记录在record{content:str}中

  2. 根据每个任务步骤，调用合适的规则（全局规则总是执行）

  3. 根据每个任务步骤，调用合适的工具

  4. 完成任务链一环后，使用record{content:str}更新任务链
  

  ## 输出格式
  ```json
  {
    "function": "function_name",
    "parameters": {
      "param1": "value1"
    }
  }
  ```
  **输出命令使用json代码块包裹，必须包含"发送任务至服务器"消息**
  严格执行工作流程，不能偏离工作流程！！！！
  ## 用户需求如下: